{
  "name": "slack",
  "version": "1.0.0",
  "scripts": {
    "start": "node slack.js",
    "build": "yarn install && yarn build"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "2.1.1"
  }
}
